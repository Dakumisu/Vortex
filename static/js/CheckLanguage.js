class CheckLanguage {
   constructor(opt) {
      const keyboard = navigator
      console.log(keyboard);
      // this.keyboardLang = keyboard.getLayoutMap()
      // console.log(this.keyboardLang);
   }
}

export default CheckLanguage